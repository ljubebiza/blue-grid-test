import isMailValid from "./IsMailValid";
import { Alert } from "./Alert";

export default function validUsage(name, email, qusetion) {
  //   if()
  //      if (!isMailValid(email)) {
  //      Alert(
  //         "top-end",
  //         "warning",
  //         "Adress must contain name, @, domain name and domain: example@gmail.com"
  //      );
  //      return false;
  //   }

  return true;
}
